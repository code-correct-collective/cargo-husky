//! The core library of the application to create git hooks and run tasks/commands when those hook scripts execute.

/// The cli arguments used by the executable
pub mod cli;

/// The core library.
pub mod husky;
